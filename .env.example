# ========================================
# MiD Diary - Environment Variables
# ========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to Git — it contains sensitive credentials
# ========================================

# ========== FRONTEND (Vite) ==========
# API base URL for production
# For local development: http://localhost:3000/api
# For production: https://your-backend-worker.workers.dev/api
VITE_API_BASE=http://localhost:3000/api

# ========== BACKEND (Node.js) ==========
# Server port
PORT=3000

# ========== DATABASE (PlanetScale / MySQL) ==========
# Database host (PlanetScale cloud endpoint or local localhost)
DB_HOST=localhost

# Database username
DB_USER=root

# Database password
DB_PASSWORD=

# Database name
DB_NAME=mid_diary

# ========== AUTHENTICATION ==========
# JWT secret for signing tokens (use a strong random string)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ========== CORS & ALLOWED ORIGINS ==========
# Comma-separated list of allowed origins for CORS
# For development: http://localhost:5173,http://localhost:3001
# For production: https://your-app.pages.dev,https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3001

# ========== FILE UPLOADS (Cloudinary) ==========
# Optional: Use for image uploads instead of local storage
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ========== DEVELOPMENT ==========
# Set to 'development' or 'production'
NODE_ENV=development
